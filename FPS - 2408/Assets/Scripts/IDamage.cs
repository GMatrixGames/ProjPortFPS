public interface IDamage
{
    /// <summary>
    /// Damage the inheritor by given amount.
    /// </summary>
    /// <param name="amount">Amount of damage</param>
    void TakeDamage(int amount);
}

